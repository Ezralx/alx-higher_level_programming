# The ``1-my_list.py`` module
============================================
How to use 1-my_list.py
============================================

This library has one function ``print_sorted()``

``print_sorted`` returns a list of integers sorted in ascending order.

Importing the function:
    >>> MyList = __import__('1-my_list').MyList

checking for if MyList is a subclass and/or instance of list.
    >>> my_list = MyList()
    >>> issubclass(MyList, list)
    True

    >>> my_list = MyList()
    >>> isinstance(my_list, list)
    True

checking for if 'print_sorted' is an attribute of class MyList.
    >>> my_list = MyList()
    >>> 'print_sorted' in dir(MyList)
    True

cheking for  a list of positive numbers.
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

checking for  a list of repeated numbers.
    >>> my_list = MyList()
    >>> my_list.append(2)
    >>> my_list += [2]
    >>> my_list.append(2)
    >>> my_list.append(7)
    >>> my_list += [2]
    >>> my_list.print_sorted()
    [2, 2, 2, 7, 2]

checking for a list of negative numbers.
    >>> my_list = MyList()
    >>> my_list.append(-1)
    >>> my_list.append(-2)
    >>> my_list.append(-4)
    >>> my_list.append(-5)
    >>> my_list.append(-3)
    >>> my_list.print_sorted()
    [-5, -4, -3, -2, -1]

checking for a list of positive and negative numbers.
    >>> my_list = MyList()
    >>> my_list.append(-1)
    >>> my_list.append(5)
    >>> my_list.append(-2)
    >>> my_list.append(3)
    >>> my_list.append(4)
    >>> my_list.print_sorted()
    [-2, -1, 3, 4, 5]

cheking for a list containing only one number.
    >>> my_list = MyList()
    >>> my_list.append(2)
    >>> my_list.print_sorted()
    [2]

    >>> my_list = MyList()
    >>> my_list += [1]
    >>> my_list.print_sorted()
    [1]

checking for  a list of positive string numbers.
    >>> my_list = MyList()
    >>> my_list.append("1")
    >>> my_list.append("4")
    >>> my_list.append("2")
    >>> my_list.append("3")
    >>> my_list.append("5")
    >>> my_list.print_sorted()
    ['1', '2', '3', '4', '5']

checking a list of negative string numbers.
    >>> my_list = MyList()
    >>> my_list.append("-1")
    >>> my_list.append("-4")
    >>> my_list.append("-2")
    >>> my_list.append("-3")
    >>> my_list.append("-5")
    >>> my_list.print_sorted()
    ['-1', '-2', '-3', '-4', '-5']

checking for a list of positive and negative string numbers.
    >>> my_list = MyList()
    >>> my_list.append("-1")
    >>> my_list.append("4")
    >>> my_list.append("-2")
    >>> my_list.append("3")
    >>> my_list.append("-5")
    >>> my_list.print_sorted()
    ['-1', '-2', '-5', '3', '4']

checking for a list of positive characters.
    >>> my_list = MyList()
    >>> my_list.append('a')
    >>> my_list.append('b')
    >>> my_list.append('c')
    >>> my_list.append('d')
    >>> my_list.append('e')
    >>> my_list.print_sorted()
    ['a', 'b', 'c', 'd', 'e']

checking for a list of negative characters.
    >>> my_list = MyList()
    >>> my_list.append('-j')
    >>> my_list.append('-g')
    >>> my_list.append('-i')
    >>> my_list.append('-h')
    >>> my_list.append('-f')
    >>> my_list.print_sorted()
    ['-f', '-g', '-h', '-i', '-j']

checking for  a list of positive and negative characters.
    >>> my_list = MyList()
    >>> my_list.append('j')
    >>> my_list.append('-c')
    >>> my_list.append('z')
    >>> my_list.append('-g')
    >>> my_list.append('-a')
    >>> my_list.print_sorted()
    ['-a', '-c', '-g', 'j', 'z']

checking for an empty list.
    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []

checking for passing more than one argument to sort method.
    >>> my_list = MyList()
    >>> my_list.print_sorted([1, 2])
    Traceback (most recent call last):
    	...
    TypeError: print_sorted() takes 1 positional argument but 2 were given

    >>> my_list = MyList()
    >>> my_list.print_sorted([1, 2], [3, 4])
    Traceback (most recent call last):
    	...
    TypeError: print_sorted() takes 1 positional argument but 3 were given

checking for  the type of the print_sorted method.
    >>> my_list = MyList()
    >>> type(my_list.print_sorted)
    <class 'method'>

checking for  the printable string.
    >>> my_list = MyList()
    >>> my_list.append(7)
    >>> print(my_list)
    [7]

checking fore if print_sorted() returns a new list.
    >>> my_list = MyList()
    >>> my_list.append(4)
    >>> my_list.append(10)
    >>> my_list.append(9)
    >>> my_list.print_sorted()
    [4, 9, 10]
    >>> my_list
    [4, 10, 9]
